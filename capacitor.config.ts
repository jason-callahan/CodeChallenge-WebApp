import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.callahan.worldclock88',
  appName: 'World Clock 88',
  webDir: 'dist'
};

export default config;
